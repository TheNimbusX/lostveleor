namespace Game.Sim
{
    // Фракция задаёт враждебность, а вид — тело и правила конкретного моба.
    public enum EnemyKind : byte
    {
        None = 0,
        ForestGuardian = 1,
        ForestRootSwarm = 2,
    }
}
